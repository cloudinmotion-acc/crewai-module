# namespace package for runtime-provided agents
# This directory is intentionally left minimal; actual implementations
# may be supplied by upstream packages.  For our lightweight module we
# provide a simple CrewAIAgent stub so that imports succeed without
# pulling in the full runtime.
