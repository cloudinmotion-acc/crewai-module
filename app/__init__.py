# top-level namespace package
# The repository root often adds this directory to PYTHONPATH so that
# imports like ``import crew`` and ``import app.agents`` resolve
# correctly.  This file may be empty; it merely marks ``app`` as a
# package on Python versions that don't automatically consider namespace
# directories.
